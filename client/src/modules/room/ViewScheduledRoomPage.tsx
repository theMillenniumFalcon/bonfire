import React from "react";
import { MainWrapper } from "../wrappers/MainWrapper";

export interface ViewScheduledRoomPageProps { }

export const ViewScheduledRoomPage: React.FC<ViewScheduledRoomPageProps> = ({ }) => {
    return (
        <MainWrapper>
            Hello
        </MainWrapper>
    )
}