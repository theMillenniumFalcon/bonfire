import React from "react";
import { RoomCategory } from "./RoomCategory";

interface RoomCategoriesProps { }

export const RoomCategories: React.FC<RoomCategoriesProps> = ({ }) => {
    return (
        <RoomCategory />
    )
}