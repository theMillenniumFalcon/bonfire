import { Box } from "@chakra-ui/react";
import React from "react";

export interface RightSectionContentHeadingProps { }

export const RightSectionContentHeading: React.FC<RightSectionContentHeadingProps> = ({ }) => {
    return (
        <Box>
            Hello
        </Box>
    )
}